{
  "name": "painters-and-paints",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/build-worker.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:vercel": "vite build --mode vercel"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.1.0",
    "jsdom": "^26.0.0",
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  }
}
