// Use an international number, digits only (for example country code + number).
// Leave blank until the real business number and service area are confirmed.
export const business = Object.freeze({
  name: 'Painters & Paints',
  whatsappNumber: '',
  serviceArea: ''
});
